<%- include('../partials/header') %>

<table id="list">
  <thead>
    <tr>
      <th>Flight</th>
      <th>Depature<br>Dates</th>
      <th></th>
    </tr>
  </thead>
  <tbody>
    <% flights.forEach(function(m) { %>
      <tr>
        <td><%= m.flight %></td>
        <td><%= m.depatures %></td>
        <td><a href="/flights/<%= m._id %>">DETAILS</a></td>
      </tr>
      <% }); %>
    </tbody>
  </table>
  
<%- include('../partials/footer') %>
